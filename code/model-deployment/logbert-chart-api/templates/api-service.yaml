apiVersion: v1
kind: Service
metadata:
  name: logbert-api-o
spec:
  type: ClusterIP
  selector:
    app: logbert-api-o
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.service.port }}
